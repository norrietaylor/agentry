Type: cli
Command: agentry keygen --private-key /tmp/signing-key.pem --public-key /tmp/public-key.pem
Expected: Exit code 0, both key files created, private key mode 0o600, instructions to commit public key shown
Timestamp: 2026-03-20T00:00:00Z
Status: PASS

--- Invocation via Click test runner (paths redacted to /tmp for clarity) ---

Exit code: 0

Output:
  Private key written to: /tmp/signing-key.pem
  Public key written to:  /tmp/public-key.pem

  Next steps:
    git add /tmp/public-key.pem
    git commit -m 'chore: add agentry public signing key'

  Keep your private key safe. It should NEVER be committed to the repository.

Key file checks:
  Private key exists: True
  Public key exists: True
  Private key mode: 0o600 (owner-read/write only)

Verification:
  Both key files are valid PEM-encoded Ed25519 keys.
  sign/verify round-trip succeeded.

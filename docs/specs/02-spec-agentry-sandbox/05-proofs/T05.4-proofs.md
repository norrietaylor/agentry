# T05.4 Proof Summary

Task: Implement 'agentry validate --security-audit' for workflow diffing

## Implementation

- `src/agentry/security/audit.py` -- New module providing `security_audit()`,
  `security_audit_single()`, `SecurityAuditReport`, and `FieldDiff`.
- `src/agentry/cli.py` -- Updated `validate` command to accept `--security-audit`
  flag with optional two-path diff mode.
- `tests/unit/test_security_audit.py` -- 57 new unit tests covering all behaviour.

## Proof Artifacts

| File | Type | Result |
|------|------|--------|
| T05.4-01-test.txt | unit tests | PASS (57/57) |
| T05.4-02-cli.txt | CLI help output | PASS |

## Security-Relevant Fields Diffed

- `safety.trust` -- execution trust level
- `safety.network.allow` -- network domain allowlist
- `safety.filesystem.read` -- filesystem read patterns
- `safety.filesystem.write` -- filesystem write patterns
- `output.side_effects` -- declared side effects
- `output.output_paths` -- allowed output paths
- Signature presence -- warns when either workflow lacks a signature

## CLI Usage

```
agentry validate --security-audit workflows/code-review.yaml
agentry validate --security-audit workflows/v1.yaml workflows/v2.yaml
agentry validate --security-audit --output-format json workflows/v1.yaml workflows/v2.yaml
```

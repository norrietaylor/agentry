# T03.2 Proof Summary: Tests for GitHubTokenScopeCheck

## Task

Write comprehensive unit tests for `GitHubTokenScopeCheck` in `tests/unit/test_github_token_check.py`.

## Implementation

Created `/Users/norrie/code/agentry/tests/unit/test_github_token_check.py` with 37 test cases
covering all required scenarios.

### Test Classes

| Class | Tests | Coverage |
|---|---|---|
| `TestGitHubTokenScopeCheckName` | 2 | name property returns "github_token_scope" |
| `TestSkipWhenNoToken` | 5 | GITHUB_TOKEN unset/empty => passed=True with skip message |
| `TestScopeVerificationPass` | 4 | HTTP 200 => passed=True |
| `TestScopeVerificationFail` | 6 | HTTP 403 => passed=False, message identifies missing scope and tool |
| `TestToolToScopeMapping` | 8 | repository:read -> contents:read, pr:comment/pr:review -> pull-requests:write |
| `TestMultipleMissingScopes` | 3 | All missing scopes and tools listed in message |
| `TestNetworkErrorHandling` | 4 | URLError, OSError handled gracefully with passed=False |
| `TestCheckWithNoRepository` | 1 | Empty repository optimistically passes |
| `TestResultProtocol` | 4 | _CheckResult fields conform to PreflightCheckResult protocol |

## Proof Artifacts

| File | Type | Status |
|---|---|---|
| T03.2-01-test.txt | test | PASS - 37/37 tests pass |
| T03.2-02-cli.txt | cli | PASS - 1247/1247 unit tests pass, no regressions |

## Key Behaviours Verified

1. **Skip when no token**: `GITHUB_TOKEN` unset/empty => `passed=True` with skip message mentioning `GITHUB_TOKEN`
2. **403 failure**: GitHub API 403 for `pr:comment` => `passed=False`, message contains `pull-requests:write`, message identifies `pr:comment` as the requiring tool, remediation mentions `permissions: pull-requests: write` and GitHub Actions workflow YAML
3. **Tool-to-scope mapping**: `repository:read` maps to `contents:read`; `pr:comment` and `pr:review` both map to `pull-requests:write`
4. **Multiple missing scopes**: All tools requiring a scope are listed in the message
5. **Network errors**: `URLError` and `OSError` result in `passed=False` with a non-empty message

# T01.1 Proof Summary: GitHubActionsBinder class scaffold and environment variable handling

## Task

Create the `GitHubActionsBinder` class in `src/agentry/binders/github_actions.py` with
basic class structure conforming to the `EnvironmentBinder` protocol.

## Files Created / Modified

- `src/agentry/binders/github_actions.py` — new file, `GitHubActionsBinder` class
- `src/agentry/binders/__init__.py` — updated to export `GitHubActionsBinder`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.1-01-cli.txt | cli | PASS |
| T01.1-02-test.txt | test | PASS |

## Summary of Evidence

### T01.1-01-cli.txt (PASS)

Exercises the `GitHubActionsBinder` class directly via a Python script:

- Class instantiates with correct `name = "github-actions"` class attribute
- Constructor reads all five required env vars: `GITHUB_EVENT_NAME`, `GITHUB_EVENT_PATH`,
  `GITHUB_WORKSPACE`, `GITHUB_REPOSITORY`, `GITHUB_TOKEN`
- `GITHUB_EVENT_PATH` JSON payload is parsed and stored as `self._event_payload`
- PR number (42) is extracted from a `pull_request` event payload
- For non-`pull_request` events (`push`), `pr_number` returns `None`
- Missing `GITHUB_TOKEN` raises `ValueError` with actionable message
- All four stub methods (`resolve_inputs`, `bind_tools`, `map_outputs`,
  `generate_pipeline_config`) raise `NotImplementedError`
- `isinstance(binder, EnvironmentBinder)` returns `True` (protocol conformance)

### T01.1-02-test.txt (PASS)

All 40 existing `test_local_binder.py` tests pass without regressions, confirming
the new `GitHubActionsBinder` export in `__init__.py` does not break anything.

## Conclusion

All deliverables met. `GitHubActionsBinder` is scaffolded, protocol-conformant,
validates env vars with actionable errors, parses the event payload, extracts the
PR number, and stubs future methods with `NotImplementedError`.

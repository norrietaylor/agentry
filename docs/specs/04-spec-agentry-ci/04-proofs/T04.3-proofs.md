# T04.3 Proof Summary: Tests for agentry ci generate command

## Task

Write comprehensive unit tests for the `ci generate` command in `tests/unit/test_ci_generate.py`.

## Artifacts

| File | Type | Status |
|------|------|--------|
| T04.3-01-test.txt | test | PASS |
| T04.3-02-cli.txt | cli | PASS |

## Implementation

Created `/Users/norrie/code/agentry/tests/unit/test_ci_generate.py` with 50 tests across 6 test classes:

### TestDerivePermissions (8 tests)
Unit tests for `_derive_permissions()`:
- Empty capabilities defaults to `contents: read`
- `pr:comment` grants `pull-requests: write`
- `pr:comment` retains `contents: read`
- `repository:read` grants `contents: read`, no extra permissions
- `repository:write` upgrades `contents` to `write`
- Multiple capabilities are combined correctly
- No downgrade from write to read
- `issue:` prefix grants `issues: write`

### TestBuildTriggers (7 tests)
Unit tests for `_build_triggers()`:
- `pull_request` trigger produces `on.pull_request` block
- `push` trigger produces `on.push` block
- `issues` trigger produces `on.issues` block with `types` list
- `schedule` trigger with cron produces `on.schedule` with correct cron entry
- Multiple triggers produce all blocks

### TestRenderPipelineYaml (16 tests)
Unit tests for `render_pipeline_yaml()`:
- Output is valid YAML (parsed with `yaml.safe_load`)
- Name derived from workflow identity
- All four trigger types correctly rendered in `on` block
- Multiple triggers both present
- Permission derivation for `pr:comment` tool
- Permission for `repository:read` only (`contents: read`, no `pull-requests`)
- Jobs section present with `ubuntu-latest` runner
- Run step includes `agentry run` with workflow path
- Run step env includes `ANTHROPIC_API_KEY` and `GITHUB_TOKEN` secrets
- Steps include checkout and setup-python

### TestCiGenerateDryRun (9 tests)
CLI integration tests via Click CliRunner for `--dry-run`:
- Valid YAML printed to stdout
- YAML contains `name` and `jobs` fields
- No file written to disk
- All four trigger types verified in `on` block
- Multiple triggers (pull_request + schedule) both present

### TestCiGenerateFileOutput (6 tests)
CLI integration tests for file output:
- File written at `<output_dir>/agentry-<name>.yaml`
- Written file is valid YAML
- Generated path mentioned in stdout
- Parent directory created automatically
- Permission for `pr:comment` tool in written file
- `repository:read` only permission in written file

### TestCiGenerateErrors (4 tests)
CLI integration tests for error cases:
- Composed workflow rejected with prescribed error messages
- Missing `--schedule` flag when `schedule` trigger specified
- Unsupported `--target` exits 1 with target name in error
- Unsupported `--target` mentions `github` as supported value

## Results

- 50/50 tests pass
- No regressions in existing test_ci_generate_cli.py (21 tests still pass)
- Combined: 71 tests pass

Type: cli
Command: agentry ci generate --target github --triggers pull_request,schedule --schedule '0 2 * * 1' --output-dir /tmp/ci-proof/ /tmp/test-workflow.yaml
Expected: File created at /tmp/ci-proof/agentry-test-workflow.yaml with schedule trigger
Timestamp: 2026-03-21T17:30:00Z
Status: PASS

Generated: /tmp/ci-proof/agentry-test-workflow.yaml
---
Generated file contents:
name: 'Agentry: code-review'
'on':
  pull_request: {}
  schedule:
  - cron: 0 2 * * 1
permissions:
  contents: read
  pull-requests: write
jobs:
  agentry:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install agentry
      run: pip install agentry
    - name: Run agentry
      run: agentry run /tmp/test-workflow.yaml
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

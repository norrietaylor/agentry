# T03.2 Proof Summary: Integrate failure policies into CompositionEngine

## Task
Modify `src/agentry/composition/engine.py` to integrate failure policy handling
(abort, skip, retry) into the node execution loop.

## Changes Made
- Imported `CompositionAbortError`, `NodeFailure`, `handle_abort`, `handle_skip`,
  `handle_retry` from `agentry.composition.failure`
- Added `_node_outputs` dict for downstream failure propagation (skip policy)
- Added `_live_record` reference for abort handler status updates
- Added `_aborted` flag to halt scheduling loop on abort
- Replaced placeholder `_apply_failure_policy` with real dispatch to abort/skip/retry
- Extracted `_fire_node_fail_callback` helper to reduce duplication
- Updated `_compute_overall_status`: FAILED on abort, PARTIAL on skip+completed mix

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T03.2-01-test.txt | PASS |
| 2 | cli  | T03.2-02-cli.txt  | PASS |
| 3 | file | T03.2-03-file.txt | PASS |

## Verification
- All 24 existing composition engine tests pass (no regressions)
- Ruff lint passes with no issues
- Module imports successfully

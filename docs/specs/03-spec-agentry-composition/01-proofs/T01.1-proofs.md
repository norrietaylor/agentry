# T01.1 Proof Summary

## Task

Extend `CompositionStep` with `id`, `failure` (FailurePolicy), and `inputs` fields. Add `node_ids` helper property to `CompositionBlock`.

## File Modified

`src/agentry/models/composition.py`

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T01.1-01-cli.txt | Functional tests (backward compat + new fields) | PASS |
| T01.1-02-cli.txt | Lint (ruff) + type check (mypy) | PASS |

## Summary

- Added `FailurePolicy` Pydantic model with `mode`, `max_retries`, and `fallback` fields
- Extended `CompositionStep` with `id: str | None = None`, `failure: FailurePolicy`, and `inputs: dict[str, str]`
- Added `node_id` property on `CompositionStep` (resolves to `id` if set, else `name`)
- Added `node_ids` property on `CompositionBlock` returning list of all resolved step IDs
- Backward compatibility confirmed: existing composition steps with only `name`, `workflow`, `depends_on` parse without errors
- `extra="forbid"` and `strict=True` remain enforced
- ruff and mypy both pass with no issues

# T02.2 Proof Artifacts: TTY-aware output formatting and progress display

## Task Summary

Implemented `src/agentry/output.py` providing:
- `emit()` — TTY-aware color-coded text output; structured JSON when piped
- `Spinner` — animated progress indicator with elapsed time for TTY mode
- `InterruptHandler` — SIGINT context manager that prints partial results and exits 130

Created `tests/unit/test_output_formatting.py` with 30 comprehensive tests.

## Proof Files

| File | Type | Status |
|------|------|--------|
| T02.2-01-test.txt | pytest test run | PASS |
| T02.2-02-cli.txt | module smoke test | PASS |

## Evidence Summary

### T02.2-01-test.txt — pytest (30 tests)

All 30 unit tests pass covering:
- `emit()`: text/JSON/auto format modes, color codes per level (critical=red, warning=yellow, info=blue, success=green), no ANSI on non-TTY
- `Spinner`: no-op on non-TTY, activates on TTY, writes label + elapsed time, idempotent stop
- `InterruptHandler`: restores signal on clean exit, text/JSON partial output on SIGINT, exits 130
- CLI integration: validate JSON, run JSON/text output, SIGINT → exit 130

### T02.2-02-cli.txt — module smoke test

Confirmed `emit()`, `Spinner`, and `InterruptHandler` work as expected from the Python REPL.

## Files Created/Modified

- **Created**: `src/agentry/output.py`
- **Created**: `tests/unit/test_output_formatting.py`
- **Created**: `docs/specs/01-spec-agentry-cli/02-proofs/T02.2-01-test.txt`
- **Created**: `docs/specs/01-spec-agentry-cli/02-proofs/T02.2-02-cli.txt`

# T02.1 Proof Summary: Click CLI Group with Subcommands and Global Options

**Task**: T02.1 — Click CLI group with subcommands and global options
**Date**: 2026-03-20
**Status**: PASS

## Implementation Summary

Implemented the full Click-based CLI at `src/agentry/cli.py` with:

- **Click group** (`main`/`cli`) with `--version`, `--verbose`, `--config`, `--output-format` global options
- **Subcommands**: `run`, `validate`, `setup`, `ci`, `registry`
- **Stub commands** (`setup`, `ci`, `registry`): print "Not yet implemented", exit 0
- **validate command**: wires to `agentry.parser.validate_workflow_file` when available; falls back to YAML syntax check; exits 0 on success, 1 on failure
- **run command**: accepts repeatable `--input KEY=VALUE`, `--target PATH`; SIGINT handler exits 130; wires to `agentry.executor` when available; stub JSON/text output otherwise
- **`--help`** on every command with usage examples
- **Entry point** wired in `pyproject.toml`: `agentry = "agentry.cli:cli"`
- **`cli` alias** = `main` so both `agentry.cli:cli` and `agentry.cli:main` work

## Proof Artifacts

| File | Type | Status | Description |
|------|------|--------|-------------|
| T02.1-01-cli.txt | cli | PASS | `agentry --help` lists all 5 commands with global options |
| T02.1-02-cli.txt | cli | PASS | `agentry validate --help` shows examples and exit codes |
| T02.1-03-test.txt | test | PASS | 31/31 unit tests pass |

## Key Test Coverage

- CLI group help lists: run, validate, setup, ci, registry
- Global options: --verbose, --config, --output-format (auto/json/text)
- Stub commands exit 0 and print "Not yet implemented"
- validate: missing file exits 1, valid YAML exits 0, parser wiring, JSON format output
- run: missing file exits 1, invalid --input exits 1, --target accepted, JSON stub output
- Entry point: `cli` is a `click.Group`, `cli is main`

## Files Modified

- `src/agentry/cli.py` — full CLI implementation
- `tests/unit/test_cli.py` — 31 unit tests
- `pyproject.toml` — entry point updated to `agentry.cli:cli`

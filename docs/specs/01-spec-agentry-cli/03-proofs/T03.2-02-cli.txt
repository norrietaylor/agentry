Type: cli
Command: python3 -c "from agentry.binders import LocalBinder; binder = LocalBinder(); ..."
Expected: git-diff resolves to string, repository-ref returns absolute path, map_outputs produces paths under .agentry/runs/
Timestamp: 2026-03-20T00:00:00Z
Status: PASS

--- Output ---
git-diff result type: str
git-diff contains hello.py: True

repository-ref result: /private/var/folders/.../tmp...
is absolute: True
output path ends with output.json: True
execution_record path ends with execution-record.json: True
runs dir in path: True

--- Summary ---
All three core T03.2 features verified:
1. _resolve_git_diff() runs subprocess git diff and returns output as a string
2. _resolve_repository_ref() returns the absolute path of the validated git repo
3. map_outputs() produces .agentry/runs/<run_id>/output.json and execution-record.json paths

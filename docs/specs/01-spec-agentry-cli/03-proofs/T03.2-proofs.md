# T03.2 Proof Summary

**Task**: T03.2 - Input resolution (git-diff, repository-ref) and output mapping
**Status**: PASS
**Completed**: 2026-03-20
**Model**: sonnet

## Implementation

Modified `src/agentry/binders/local.py`:
- Implemented `_resolve_git_diff()`: runs `git diff <ref>` via `subprocess.run()` in the target directory and returns stdout as a string. Validates target is a git repo first via `_assert_git_repo()`.
- `_resolve_repository_ref()` was already functional from T03.1; returns resolved absolute path.
- `map_outputs()` already mapping to `.agentry/runs/<run_id>/` from T03.1.

Updated `tests/unit/test_local_binder.py`:
- Replaced T03.1 skeleton test `test_git_diff_on_git_dir_raises_not_implemented` with three real tests that verify actual git diff output, diff content, and empty diff behavior.

Created `tests/unit/test_input_resolution.py`:
- 19 new tests covering git-diff resolution, repository-ref, output mapping, and CLI input parsing.
- Uses `git_repo` fixture that creates a real temporary git repository via subprocess.

## Proof Artifacts

| File | Type | Status |
|------|------|--------|
| T03.2-01-test.txt | test | PASS - 59 tests pass |
| T03.2-02-cli.txt | cli | PASS - runtime verification |

## Test Results

- 59 tests in `test_input_resolution.py` and `test_local_binder.py`: **59 PASSED**
- Full suite: **352 PASSED, 0 FAILED**

## Key Behaviors Verified

1. `git diff HEAD~1` produces string output containing changed filenames and diff hunks
2. `git diff HEAD` returns empty string when working tree is clean
3. Non-git directories raise `NotAGitRepositoryError` with the bad path in the message
4. `repository-ref` returns `os.path.isabs()` verified absolute path matching `Path.resolve()`
5. Output paths are nested under `<target_dir>/.agentry/runs/<run_id>/`
6. `output.json` and `execution-record.json` are always present in output mapping
7. CLI `--input diff=HEAD~1` is split on first `=` producing `{"diff": "HEAD~1"}`

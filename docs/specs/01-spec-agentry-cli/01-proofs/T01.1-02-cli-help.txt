Type: cli
Command: agentry --help
Expected: CLI shows all available commands with descriptions
Timestamp: 2026-03-20T00:00:00Z

Usage: agentry [OPTIONS] COMMAND [ARGS]...

  Agentry: Portable agentic workflow orchestration.

  Treats agentic workflows as portable, declarative definitions that run
  identically on a developer's laptop and in CI.

  Examples:
    agentry validate workflows/code-review.yaml
    agentry run workflows/code-review.yaml --input diff=HEAD~1
    agentry --verbose run workflows/code-review.yaml --input diff=HEAD~1

  Run 'agentry COMMAND --help' for help on a specific command.

Options:
  --version                       Show the version and exit.
  -v, --verbose                   Increase log level to DEBUG. Debug messages
                                  are written to stderr.
  -c, --config PATH               Override the default config file location.
  -f, --output-format [auto|json|text]
                                  Force output format. 'auto' detects TTY:
                                  human-readable when interactive, JSON when
                                  piped. 'json' always emits JSON. 'text'
                                  always emits human-readable text.  [default:
                                  auto]
  --help                          Show this message and exit.

Commands:
  ci        Generate CI pipeline configuration for a workflow.
  registry  Browse and manage the workflow registry.
  run       Execute a workflow definition against a local repository.
  setup     Set up Agentry for the current environment.
  validate  Validate a workflow definition YAML file.

Status: PASS

# T01.1 Proof Summary

## Task: Build single-workflow execution pipeline in cli.py

### Changes Made

Replaced the stub at lines 718-736 of `src/agentry/cli.py` with a real pipeline
invocation that:

1. Resolves agent runtime and kwargs from the workflow's agent block
2. Instantiates RunnerDetector with AgentRegistry.default() and gets the runner
3. Builds preflight checks respecting --skip-preflight (same pattern as setup phase)
4. Instantiates SecurityEnvelope with workflow, runner, and preflight checks
5. Resolves inputs via binder.resolve_inputs() with serialized input declarations
6. Binds tools via binder.bind_tools()
7. Loads system prompt from file (or builds fallback from identity block)
8. Calls envelope.execute() with all parameters
9. Handles EnvelopeResult: aborted/error -> stderr + sys.exit(1)
10. On success: JSON output (status, output, token_usage) or human-readable text
11. Falls back to stub output when pipeline components are unavailable (ImportError)

### Proof Artifacts

| # | File | Type | Status |
|---|------|------|--------|
| 1 | T01.1-01-test.txt | test | PASS |
| 2 | T01.1-02-lint.txt | cli (lint) | PASS |

### Verification

- All 42 existing unit tests pass (no regressions)
- ruff check passes with no violations
- ImportError fallback preserves backward compatibility with minimal YAML files

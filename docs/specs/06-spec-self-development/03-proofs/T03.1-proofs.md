# T03.1 Proof Summary

Task: Generate and commit .github/workflows/agentry-code-review.yml
Status: PASS

## Artifacts

| File | Type | Status |
|------|------|--------|
| T03.1-01-cli.txt | cli (YAML validation) | PASS |
| T03.1-02-file.txt | file (content verification) | PASS |

## Summary

Created `.github/workflows/agentry-code-review.yml` with all required fields:

1. **name**: "Agentry: Code Review"
2. **on**: pull_request (all types, no filter)
3. **permissions**: contents: read, pull-requests: write (minimal, derived from tool manifest)
4. **env**: ANTHROPIC_API_KEY from secrets (top-level job env)
5. **jobs.agentry**:
   - runs-on: ubuntu-latest
   - steps:
     a. Checkout repository (actions/checkout@v4)
     b. Set up Python 3.12 (actions/setup-python@v5)
     c. Install agentry (pip install .)
     d. Run agentry with `workflows/code-review.yaml`, `--input diff=${{ github.event.pull_request.head.sha }}~1`, `--input codebase=.`, `--binder github-actions`, `--output-format json`
   - env for run step: ANTHROPIC_API_KEY and GITHUB_TOKEN from secrets

## Notes

- The workflow file is manually crafted to match the task requirements exactly.
- The run command uses `pip install .` (install from source) rather than `pip install agentry` (from PyPI) since this is the agentry repository's own self-review workflow.
- The `'on'` key is quoted to prevent PyYAML from interpreting it as boolean `True` (YAML 1.1 quirk).
- T03.2 will verify alignment with `agentry ci generate --dry-run` output.

# T04.1 Proof Summary: Implement pr:create tool binding in LocalBinder

## Task

Add a `pr:create` tool binding to the LocalBinder that creates a branch, commits
changes, and opens a PR via `gh` CLI with safety guardrails.

## Proof Artifacts

| # | Type | File | Status |
|---|------|------|--------|
| 1 | test | T04.1-01-test.txt | PASS |
| 2 | cli  | T04.1-02-cli.txt  | PASS |
| 3 | file | T04.1-03-file.txt | PASS |

## Details

### T04.1-01-test.txt (Unit Tests)
- Ran `python3 -m pytest tests/unit/test_local_binder.py -x -v`
- All 40 existing tests pass, confirming no regressions

### T04.1-02-cli.txt (Linter)
- Ran `ruff check src/agentry/binders/local.py`
- All checks passed

### T04.1-03-file.txt (Implementation Verification)
- Verified 11 implementation requirements:
  1. `pr:create` added to `SUPPORTED_TOOLS`
  2. `_make_pr_create()` returns a callable
  3. Callable has correct `__name__`
  4. Signature matches spec (branch_name, commit_message, base_branch, title, body, label, files, cwd)
  5. Default values correct (base_branch="main", label="agent-proposed", files=None, cwd=None)
  6. Protected branch guard blocks "main"
  7. Protected branch guard blocks "master"
  8. `bind_tools()` wires pr:create correctly
  9. Unsupported tools (pr:comment) still rejected
  10. shell:execute allowlist unchanged (remains read-only)
  11. Graceful failure returns structured dict

## Changes Made

- `src/agentry/binders/local.py`:
  - Added `"pr:create"` to `SUPPORTED_TOOLS` frozenset
  - Added `_PROTECTED_BRANCHES` constant
  - Added `_make_pr_create()` factory function
  - Wired `pr:create` in `bind_tools()` method
